const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');
const workflow=fs.readFileSync('.github/workflows/produccion.yml','utf8');const pkg=require('../package.json');
function pos(x){const i=workflow.indexOf(x);assert.ok(i>=0,'Falta '+x);return i;}
test('R33 certification contract: explicit Evidence canonical and accessibility gates precede predeploy',()=>{const pre=pos('npm run predeploy');for(const step of ['npm run test:chat-editorial','npm run test:evidence','npm run test:canonical-tools','npm run qa:baseline'])assert.ok(pos(step)<pre,step+' debe ejecutarse antes de predeploy');});
test('R33 certification contract: deploy remains manual main-only',()=>{assert.match(workflow,/github\.event_name == 'workflow_dispatch' && github\.ref == 'refs\/heads\/main'/);assert.equal((workflow.match(/run: npm run deploy/g)||[]).length,1);});
test('R33 certification contract: Evidence suite names every Foundation test module',()=>{const script=pkg.scripts['test:evidence'];for(const file of ['evidence foundation.test.cjs','evidence registry.test.cjs','evidence claims.test.cjs','evidence validator.test.cjs','evidence apa.test.cjs','evidence reviews.test.cjs','evidence api.test.cjs','evidence runtime integration.test.cjs','evidence policies provenance.test.cjs','evidence boundaries.test.cjs'])assert.ok(script.includes(file),'Falta '+file);});
test('R33 certification contract: production deploy is never a pull request step',()=>{const deployBlock=workflow.slice(workflow.indexOf('- name: Desplegar en produccion'),workflow.indexOf('- name: Sincronizar credenciales D1 Analytics'));assert.match(deployBlock,/workflow_dispatch/);assert.match(deployBlock,/refs\/heads\/main/);});
