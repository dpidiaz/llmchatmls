const test=require('node:test');const assert=require('node:assert/strict');const apa=require('../MLS R32 EDITORIAL/evidence apa.js');
const base={status:'active',authorityTier:'B',authors:['Doe, Jane Marie'],publicationYear:2025};
test('APA: personal authors render surname and initials',()=>{assert.equal(apa.referencePerson('Doe, Jane Marie'),'Doe, J. M.');});
test('APA: two authors use ampersand in reference list',()=>{assert.equal(apa.joinReferenceAuthors(['Doe, Jane','Roe, Richard']),'Doe, J., & Roe, R.');});
test('APA: more than 20 authors uses first 19 ellipsis last',()=>{const a=Array.from({length:21},(_,i)=>'Family'+i+', Name');const out=apa.joinReferenceAuthors(a);assert.ok(out.includes('… Family20, N.'));assert.ok(!out.includes('& Family20'));});
test('APA: Spanish URL profile formats webpage date in Spanish',()=>{assert.equal(apa.referenceDate({publicationDate:'2025-09-21'},'es-GT'),'(2025, 21 de septiembre).');});
test('APA: book requires publisher and renders DOI as https URL',()=>{const src={...base,sourceType:'book',title:'Gramática descriptiva',publisher:'Editorial X',doi:'10.1000/xyz.123'};const r=apa.renderApaReference(src);assert.equal(r.citationReady,true);assert.match(r.markdown,/Doe, J\. M\. \(2025\)\. \*Gramática descriptiva\*\. Editorial X\. https:\/\/doi\.org\/10\.1000\/xyz\.123$/);});
test('APA: book chapter fails closed without editors',()=>{const src={...base,sourceType:'book_chapter',title:'Capítulo',containerTitle:'Libro',publisher:'Editorial',pages:'1–20'};assert.throws(()=>apa.renderApaReference(src),e=>e.code==='APA_METADATA_INVALID'&&e.errors.includes('editors_required'));});
test('APA: book chapter renders editors and pages',()=>{const src={...base,sourceType:'book_chapter',title:'Capítulo',containerTitle:'Libro de lingüística',editors:['Smith, Alex'],publisher:'Editorial',pages:'1–20'};assert.match(apa.renderApaReference(src).markdown,/Capítulo\. In A\. Smith \(Ed\.\), \*Libro de lingüística\* \(pp\. 1–20\)\. Editorial\.$/);});
test('APA: journal article needs pages or article number',()=>{const src={...base,sourceType:'journal_article',title:'Artículo',journal:'Revista',volume:'3',issue:'2'};assert.equal(apa.validateApaSource(src).citationReady,false);assert.ok(apa.validateApaSource(src).errors.includes('pages_or_article_number_required'));});
test('APA: journal article renders journal and volume together in italics',()=>{const src={...base,sourceType:'journal_article',title:'Artículo',journal:'Revista',volume:'3',issue:'2',pages:'10–20',doi:'10.1000/test'};assert.match(apa.renderApaReference(src).markdown,/Artículo\. \*Revista, 3\*\(2\), 10–20\. https:\/\/doi\.org\/10\.1000\/test$/);});
test('APA: institutional webpage omits repeated site name',()=>{const src={status:'active',sourceType:'institutional_webpage',title:'Guía APA',institution:'Universidad X',publisher:'Universidad X',publicationDate:'2025-09-21',canonicalUrl:'https://example.org/guide'};const r=apa.renderApaReference(src);assert.equal((r.markdown.match(/Universidad X/g)||[]).length,1);assert.ok(r.markdown.endsWith('https://example.org/guide'));});
test('APA: unresolved source can render but is not citationReady',()=>{const src={...base,status:'unresolved',sourceType:'book',title:'Libro',publisher:'Editorial'};const r=apa.renderApaReference(src);assert.equal(r.valid,true);assert.equal(r.citationReady,false);assert.ok(r.warnings.includes('source_not_active'));});
test('APA: in-text one two and three authors follow APA author shortening',()=>{const one={...base,sourceType:'book',title:'A',publisher:'P'};assert.equal(apa.renderInTextCitation(one),'(Doe, 2025)');const two={...one,authors:['Doe, Jane','Roe, Richard']};assert.equal(apa.renderInTextCitation(two),'(Doe & Roe, 2025)');assert.equal(apa.renderInTextCitation(two,{narrative:true,locale:'es-GT'}),'Doe y Roe (2025)');const three={...one,authors:['Doe, Jane','Roe, Richard','Moe, Mary']};assert.equal(apa.renderInTextCitation(three),'(Doe et al., 2025)');});
test('APA: unsupported source type fails closed',()=>{assert.equal(apa.validateApaSource({...base,sourceType:'other',title:'X'}).citationReady,false);assert.throws(()=>apa.renderApaReference({...base,sourceType:'other',title:'X'}),e=>e.code==='APA_METADATA_INVALID');});
