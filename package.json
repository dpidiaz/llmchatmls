{
  "name": "llmchatmls",
  "description": "Retired MASTER LANGUAGE SYSTEM deployment",
  "private": true,
  "devDependencies": {
    "wrangler": "4.127.0"
  },
  "scripts": {
    "predeploy": "cp 'MLS R32 OVERLAY/index.ts' src/index.ts && cp 'MLS R32 OVERLAY/wrangler.jsonc' wrangler.jsonc",
    "deploy": "wrangler deploy"
  }
}
