name: Temporary shared Workers AI budget validation

on:
  push:
    branches:
      - feature/shared-workers-ai-budget
    paths:
      - '.github/validate-shared-workers-ai-budget-trigger'

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: node --test 'test/workers ai shared budget.test.cjs'
      - run: npm run test:chat-editorial
      - run: npm run predeploy
      - run: npm run recovery:verify
      - run: node --test test/deploy-contract.test.cjs
      - run: npm run check
